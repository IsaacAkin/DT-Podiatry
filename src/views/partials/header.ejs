<header>
    <h1><a href="/">DT Podiatry</a></h1>
    <ul>
        <li class="dropdown-container">
            <a href="/treatments">Treatments</a>
            <ul class="dropdown">
                <% for (let i = 0; i < treatmentLinks.length; i++) { %>
                    <li>
                        <a href="<%= treatmentLinks[i].href %>"><%= treatmentLinks[i].text %></a>
                    </li>
                <% } %>
            </ul>
        </li>
    <% for (let i = 0; i < navLinks.length; i++) { %>
        <li>
            <a href="<%= navLinks[i].href %>"><%= navLinks[i].text %></a>
        </li>
    <% } %>
    </ul>
</header>